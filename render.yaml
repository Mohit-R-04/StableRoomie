services:
  # 1. Java Spring Boot Backend
  - type: web
    name: stableroomie
    env: docker
    dockerfilePath: StableRoomie/Dockerfile
    dockerContext: StableRoomie
    plan: free
    healthCheckPath: /
    envVars:
      - key: GOOGLE_CLIENT_ID
        sync: false # Render will prompt you for this value during setup
      - key: GOOGLE_CLIENT_SECRET
        sync: false # Render will prompt you for this value during setup
      - key: FLASK_API_URL
        value: http://stableroomie-flask:5000

  # 2. Flask Microservice
  - type: web
    name: stableroomie-flask
    env: docker
    dockerfilePath: flask-api/Dockerfile
    dockerContext: flask-api
    plan: free
    healthCheckPath: /health
    envVars:
      - key: JAVA_BACKEND_URL
        value: http://stableroomie:8080
